#include <stdio.h>

/**
* main - Prints the alphabet in lowercase, and then in uppercase.
*
* Return: Always 0.
*/
int main(void)
{
	char letter;

	for (le = 'a'; lett++ <= 'z'; letter++)
		 putchar(letter);
	
	for (letter = 'A'; letter <= 'Z'; letter++)
	puchar(letter);
	
	putchar('\n');

	return (0)
}
